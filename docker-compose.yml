version: '3.8'

services:
  app:
    build: .
    ports:
      - "3001:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    container_name: ai-resume-builder

  # 可选：添加反向代理
  # nginx:
  #   image: nginx:alpine
  #   ports:
  #     - "80:80"
  #     - "443:443"
  #   volumes:
  #     - ./nginx-proxy.conf:/etc/nginx/conf.d/default.conf
  #     - ./ssl:/etc/nginx/ssl
  #   depends_on:
  #     - app
  #   restart: unless-stopped
